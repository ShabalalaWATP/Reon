name: CI

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "30 5 * * 1"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  root-static:
    name: Root static gates
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3 # v6.0.10
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          version: 0.12.0
      - run: pnpm install --frozen-lockfile
      - run: uv sync --project apps/api --all-groups --frozen
      - run: pnpm check
      - name: Verify recovery transport and authenticated-manifest policy
        shell: pwsh
        run: |
          $result = Invoke-Pester -Path scripts/tests/PostgresRecovery.Tests.ps1 -PassThru
          if ($result.FailedCount -ne 0) { exit 1 }
      - run: pnpm smoke-contract
      - run: pwsh -File workflow/validate-bpmn.ps1
      - run: pwsh -File workflow/test-validator.ps1
      - name: Validate GitHub Actions workflows
        run: |
          docker build --file scripts/actionlint.Dockerfile --tag istari-actionlint:ci .
          docker run --rm -v "$PWD:/repo" istari-actionlint:ci

  frontend:
    name: Frontend tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3 # v6.0.10
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @istari-service/web test
      - name: Retain frontend coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-coverage
          path: apps/web/coverage
          if-no-files-found: warn
          retention-days: 14

  backend:
    name: Backend static checks and tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_DB: istari_race_tests
          POSTGRES_USER: istari_tests
          POSTGRES_PASSWORD: synthetic-ci-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U istari_tests -d istari_race_tests"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      ISTARI_POSTGRES_TEST_URL: >-
        postgresql+asyncpg://istari_tests:synthetic-ci-password@127.0.0.1:5432/istari_race_tests
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          version: 0.12.0
      - run: uv sync --project apps/api --all-groups --frozen
      - run: uv run --directory apps/api ruff format --check .
      - run: uv run --directory apps/api ruff check .
      - run: uv run --directory apps/api mypy
      - run: uv run --directory apps/api bandit -c pyproject.toml -r src alembic
      - run: uv run --directory apps/api pytest
      - name: Verify mandatory PostgreSQL race tests do not skip
        shell: bash
        run: |
          set -euo pipefail
          report="${RUNNER_TEMP}/postgres-races.xml"
          uv run --directory apps/api pytest \
            tests/test_planning_concurrency.py \
            tests/test_request_participant_postgres_locking.py \
            tests/test_password_assistance_postgres_locking.py \
            tests/test_legal_hold_postgres_permissions.py \
            --no-cov --junitxml="$report"
          python - "$report" <<'PY'
          import sys
          import xml.etree.ElementTree as ET

          root = ET.parse(sys.argv[1]).getroot()
          skipped = sum(int(suite.get("skipped", "0")) for suite in root.iter("testsuite"))
          if skipped:
              raise SystemExit(f"Mandatory PostgreSQL race tests skipped: {skipped}")
          PY
      - name: Retain backend coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-coverage
          path: |
            apps/api/coverage.json
            apps/api/coverage.xml
          if-no-files-found: warn
          retention-days: 14

  dependency-audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3 # v6.0.10
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm audit --audit-level=high
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          version: 0.12.0
      - run: uv sync --project apps/api --all-groups --frozen
      - run: pnpm licence-check
      - name: Export locked runtime dependencies
        run: >-
          uv export --project apps/api --locked --all-groups
          --no-emit-project --format requirements-txt --quiet
          --output-file "${RUNNER_TEMP}/api-requirements.txt"
      - run: >-
          uv run --directory apps/api pip-audit --strict
          --requirement "${RUNNER_TEMP}/api-requirements.txt"

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Stage the exact tracked-source inventory
        run: node scripts/stage-tracked-source.mjs "${RUNNER_TEMP}/tracked-source"
      - name: Scan current source with Gitleaks
        shell: bash
        run: |
          set -euo pipefail
          docker build \
            --file "${RUNNER_TEMP}/tracked-source/scripts/secret-scan.Dockerfile" \
            --target evidence --output type=local,dest=secret-scan-evidence \
            "${RUNNER_TEMP}/tracked-source"
          cp "${RUNNER_TEMP}/tracked-source/tracked-source-inventory.txt" \
            secret-scan-evidence/tracked-source-inventory.txt
      - name: Scan reachable history with exact expiring exceptions
        run: >-
          docker build --file scripts/trufflehog-scan.Dockerfile
          --target evidence --output type=local,dest=history-scan-evidence .
      - name: Retain secret-scan evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: secret-scan-evidence
          path: |
            secret-scan-evidence
            history-scan-evidence
          if-no-files-found: warn
          retention-days: 14

  dependency-review:
    name: Pull request dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high

  codeql:
    name: CodeQL (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript, python]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: github/codeql-action/init@5595ccaf912efad79be6eef63a5619ff05969be3 # v4
        with:
          languages: ${{ matrix.language }}
          queries: security-extended
      - uses: github/codeql-action/analyze@5595ccaf912efad79be6eef63a5619ff05969be3 # v4

  postgres-component:
    name: PostgreSQL bootstrap component
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: shellcheck infra/postgres/*.sh
      - run: bash infra/postgres/test-init-databases.sh

  camunda-smoke:
    name: Bounded Camunda V2 smoke
    needs: [root-static, postgres-component]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      COMPOSE_PROJECT_NAME: istari-camunda-smoke
      APP_DATABASE_NAME: app_smoke
      APP_DATABASE_USER: app_smoke_owner
      APP_RUNTIME_DATABASE_USER: app_smoke
      APP_BACKUP_DATABASE_USER: app_smoke_backup
      APP_MAINTENANCE_DATABASE_USER: app_smoke_maintenance
      CAMUNDA_DATABASE_NAME: camunda_smoke
      CAMUNDA_DATABASE_USER: camunda_smoke
      ENVIRONMENT: local
      SESSION_COOKIE_SECURE: "false"
      WEB_ORIGIN: http://127.0.0.1:5173
      ALLOW_DEMO_USERS: "true"
      SECURITY_PSEUDONYM_KEY: ci-smoke-pseudonym-separate-at-least-32-bytes
      POSTGRES_HOST_PORT: "55432"
      CAMUNDA_HOST_PORT: "18080"
      CAMUNDA_MANAGEMENT_HOST_PORT: "19600"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Generate job-scoped credentials
        shell: bash
        run: |
          set -euo pipefail
          admin_password="$(openssl rand -hex 24)"
          app_password="$(openssl rand -hex 24)"
          runtime_password="$(openssl rand -hex 24)"
          backup_password="$(openssl rand -hex 24)"
          maintenance_password="$(openssl rand -hex 24)"
          camunda_password="$(openssl rand -hex 24)"
          demo_password="$(openssl rand -hex 24)"
          audit_key="$(openssl rand -hex 32)"
          migration_url="postgresql+asyncpg://app_smoke_owner:${app_password}@postgres:5432/app_smoke"
          database_url="postgresql+asyncpg://app_smoke:${runtime_password}@postgres:5432/app_smoke"
          for value in "$admin_password" "$app_password" "$runtime_password" "$backup_password" "$maintenance_password" "$camunda_password" "$demo_password" "$audit_key" "$migration_url" "$database_url"; do
            printf '::add-mask::%s\n' "$value"
          done
          {
            printf 'POSTGRES_ADMIN_PASSWORD=%s\n' "$admin_password"
            printf 'APP_DATABASE_PASSWORD=%s\n' "$app_password"
            printf 'APP_RUNTIME_DATABASE_PASSWORD=%s\n' "$runtime_password"
            printf 'APP_BACKUP_DATABASE_PASSWORD=%s\n' "$backup_password"
            printf 'APP_MAINTENANCE_DATABASE_PASSWORD=%s\n' "$maintenance_password"
            printf 'CAMUNDA_DATABASE_PASSWORD=%s\n' "$camunda_password"
            printf 'DEMO_USER_PASSWORD=%s\n' "$demo_password"
            printf 'AUDIT_HMAC_KEY=%s\n' "$audit_key"
            printf 'MIGRATION_DATABASE_URL=%s\n' "$migration_url"
            printf 'DATABASE_URL=%s\n' "$database_url"
          } >> "$GITHUB_ENV"
      - name: Start pinned data and workflow services
        run: >-
          timeout --signal=TERM --kill-after=10s 6m docker compose
          -f docker-compose.yml -f .github/compose.camunda-smoke.yml
          up --detach --wait --wait-timeout 330 postgres orchestration
      - name: Exercise staffed completion and unstaffed destination routes
        shell: pwsh
        run: >-
          ./scripts/smoke-camunda.ps1 -BaseUri http://127.0.0.1:18080
          -MaxAttempts 8 -RetryDelaySeconds 1
      - name: Capture bounded diagnostics
        if: failure()
        run: |
          timeout --signal=TERM --kill-after=10s 30s docker compose \
            -f docker-compose.yml -f .github/compose.camunda-smoke.yml \
            ps || true
          timeout --signal=TERM --kill-after=10s 45s docker compose \
            -f docker-compose.yml -f .github/compose.camunda-smoke.yml \
            logs --no-color --tail 150 orchestration postgres || true
      - name: Tear down smoke services
        if: always()
        run: >-
          timeout --signal=TERM --kill-after=10s 90s docker compose
          -f docker-compose.yml -f .github/compose.camunda-smoke.yml
          down --volumes --remove-orphans --timeout 20

# The bounded smoke job starts the data and workflow services only. Full product
# topology and browser journeys remain separate verification stages.
